[build]
  command = "pip install -r requirements.txt"

[deploy]
  start_command = "gunicorn app:server --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
  healthcheck_path = "/"
  
[deploy.cdn]
  enabled = false
